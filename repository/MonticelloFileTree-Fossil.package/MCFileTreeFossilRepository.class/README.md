I represent a Fossil repository for Monticello, that is a directory inside a Fossil managed repository holding multiple monticello packages.

I remap from the git superclass with the following overrides:

subDirectoryString = the repository path inside the fossil file

