initialize-release
releaseCaches
	"Release cached information, especially git-related. Done to prevent issues when moving folders containing images and git repositories."

	gitAuthorName := nil.
	gitAuthorEmail := nil.
	gitTopLevelDirectory := nil