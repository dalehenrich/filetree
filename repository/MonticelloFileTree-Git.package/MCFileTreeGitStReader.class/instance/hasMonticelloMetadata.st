testing
hasMonticelloMetadata
	"Two cases: we have either a minimal archive containing a version member or a full archive containing the metadata."
	
	"Try to avoid loading the full package :)"
	
	"Since we use a hack to avoid loading the full archive, consider this true."
	
	^ true